import React from 'react';
import toTop from '../../hooks/toTop';

const Events = () => {
    toTop()
    return (
        <div>
            sppectial Events
        </div>
    );
};

export default Events;