import React from 'react';
import toTop from '../../hooks/toTop';

const Contact = () => {
    toTop()
    return (
        <div>
            contact page
        </div>
    );
};

export default Contact;